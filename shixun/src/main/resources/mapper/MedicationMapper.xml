<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.shixun.mapper.MedicationMapper">

    <select id="findAll" resultType="com.example.shixun.model.MedicationRecord">
        SELECT mr.*, dv.generic_name as drug_name
        FROM medication_records mr
        LEFT JOIN drugs_vaccines dv ON mr.drug_id = dv.id
        ORDER BY mr.event_time DESC
    </select>

    <select id="findByEarTag" resultType="com.example.shixun.model.MedicationRecord">
        SELECT mr.*, dv.generic_name as drug_name
        FROM medication_records mr
        LEFT JOIN drugs_vaccines dv ON mr.drug_id = dv.id
        WHERE mr.ear_tag = #{earTag}
        ORDER BY mr.event_time DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO medication_records (ear_tag, drug_id, reason, event_time, dosage, operator)
        VALUES (#{earTag}, #{drugId}, #{reason}, #{eventTime}, #{dosage}, #{operator})
    </insert>

    <delete id="deleteById">
        DELETE FROM medication_records WHERE id = #{id}
    </delete>

</mapper>
