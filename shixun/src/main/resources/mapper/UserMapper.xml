<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.shixun.mapper.UserMapper">

    <select id="findAll" resultType="com.example.shixun.model.User">
        SELECT * FROM user ORDER BY id
    </select>

    <select id="findById" resultType="com.example.shixun.model.User">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <select id="findByUsername" resultType="com.example.shixun.model.User">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, age, email, phone, password, role, status)
        VALUES (#{username}, #{age}, #{email}, #{phone}, #{password}, #{role}, COALESCE(#{status}, 'active'))
    </insert>

    <update id="update">
        UPDATE user SET username=#{username}, age=#{age}, email=#{email}, phone=#{phone}
        <if test="password != null and password != ''">
            , password=#{password}
        </if>
        <if test="role != null and role != ''">
            , role=#{role}
        </if>
        <if test="status != null and status != ''">
            , status=#{status}
        </if>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM user WHERE id = #{id}
    </delete>

    <select id="findPage" resultType="com.example.shixun.model.User">
        SELECT * FROM user
        <where>
            <if test="search != null and search != ''">
                (username LIKE CONCAT('%', #{search}, '%')
                OR email LIKE CONCAT('%', #{search}, '%')
                OR phone LIKE CONCAT('%', #{search}, '%'))
            </if>
        </where>
        ORDER BY id
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countSearch" resultType="long">
        SELECT COUNT(*) FROM user
        <where>
            <if test="search != null and search != ''">
                (username LIKE CONCAT('%', #{search}, '%')
                OR email LIKE CONCAT('%', #{search}, '%')
                OR phone LIKE CONCAT('%', #{search}, '%'))
            </if>
        </where>
    </select>

    <update id="touchLastLogin">
        UPDATE user SET last_login_at=CURRENT_TIMESTAMP WHERE id=#{id} AND status='active'
    </update>

</mapper>
